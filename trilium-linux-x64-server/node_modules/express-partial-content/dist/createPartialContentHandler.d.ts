import { Request, Response } from "express";
import { Logger } from "./Logger";
import { ContentProvider } from "./ContentProvider";
export declare function createPartialContentHandler(contentProvider: ContentProvider, logger: Logger): (req: Request, res: Response) => Promise<import("express-serve-static-core").Response>;
